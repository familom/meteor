#pragma once

#include <cstdint>
#include <string>


namespace Meteor {

/// Resource is represented by it's 0-based id.
using Resource = std::size_t;

using ResourceQuantity = std::uint64_t;

} // Meteor
